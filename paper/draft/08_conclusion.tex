% §8 Conclusion

We studied pre-hoc unsupervised routing signals for multi-LLM selection: whether the model-independent and model-dependent signal families encode routing need, whether they provide complementary predictive information, and whether that prediction supports lightweight routing under controllable cost--quality preferences.
Under a frozen prompt--probe--oracle protocol, we characterized $c(q)$, $H$, and $m$ on ARC-Challenge with a prediction-then-decision evaluation structure.

\textbf{[TBD: characterization headline.]}

\textbf{[TBD: complementary predictive information ($\Delta$AUROC).]}

\textbf{[TBD: decision utility across $\lambda$ or honest negative result.]}

Our contribution is fundamental measurement of what lightweight unsupervised probes reveal---including negative results---not a new state-of-the-art router or learned pre-hoc estimator \citep{cao2026scope}.
